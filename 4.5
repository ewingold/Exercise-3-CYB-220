# Creating a list of numbers from 1 to 1000000
numbers = list(range(1, 1000001))

# Checking the list by getting the first and last number in the list
print("Minimum number in the list:", min(numbers))
print("Maximum number in the list:", max(numbers))

# Calculating the sum of the numbers in the list
sum_of_numbers = sum(numbers)
print("Sum of numbers from 1 to 1000000:", sum_of_numbers)
